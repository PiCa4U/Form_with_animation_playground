export enum Folders {
  companyName = '*Company name*',
  nit = '*NIT*',
  streetLine = '*Adress line 1 (Street and Street No)*',
  houseLine = 'Adress line 2 (Optional - Building, Warehouse)',
  postalCode = '*Postal code*',
  City = '*City*',
}
